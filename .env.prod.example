# Database (DATABASE_URL is constructed automatically in docker-compose — do not add it here)
POSTGRES_USER=ddreport
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=ddreport

# Auth
AUTH_SECRET=CHANGE_ME_RANDOM_32_CHARS

# Cloudflare Tunnel
TUNNEL_TOKEN=CHANGE_ME_FROM_CLOUDFLARE_DASHBOARD
